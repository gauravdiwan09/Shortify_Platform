spring.application.name=url-shorterner-sb

# Database connection (values from environment variables)
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/urlshortenerdb}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:Gaurav@123}

# JPA / Hibernate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update

# JWT Config
jwt.secret=${JWT_SECRET:699b589bcbf108c6c618a4486096eba0ef75af8fd5604d579ebd2f3ffaec0982}
jwt.expiration=${JWT_EXPIRATION:172800000}

# Logging
logging.level.org.springframework.security=DEBUG
logging.level.org.url.shortener=DEBUG
